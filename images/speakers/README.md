# Speaker photos

Drop headshots here with these exact filenames so `index.html` picks them up automatically:

- `andy-weightman.jpg`

Until a file is present, the site shows a circular placeholder with the speaker's initials instead.
