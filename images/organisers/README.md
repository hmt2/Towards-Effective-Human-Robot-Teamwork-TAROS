# Organiser photos

Drop headshots here with these exact filenames so `index.html` picks them up automatically:

- `hazel-taylor.jpg`
- `marie-farrell.jpg`
- `yasmeen-rafiq.jpg`

Until a file is present, the site shows a circular placeholder with the organiser's initials instead.
